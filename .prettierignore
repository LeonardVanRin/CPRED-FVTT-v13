**/.git
**/.gitlab
**/.vscode
**/dist
**/node_modules

**/foundryconfig.json.example
**/package-lock.json
**/package.json

# Crowdin-autogenerated translations — don't format. English is the source and
# IS checked, so only the per-language translation files are ignored.
**/src/babele/*/*.json
!**/src/babele/en/*.json

# These are static files, make sure to skip them
**/src/fonts/*
**/src/icons/*
**/src/maps/*
**/src/tiles/*

# Ignore templates, prettier can't handle handlebars partials
**/src/templates/*
